# Private session context (contains env URLs and bot IDs — keep local only)
CONTEXT.md

# Temp workspaces and build artifacts
_workspace_*/
_clone_*/
_inspect_*/
_push_*/
_skill_build/
_test_*/
_pb_*

# path2-vscode export output (agent YAML, configs, skill assets — not for source control)
sample/
skills-with-assets/

# PAC workspace state (env-specific, not for source control)
**/.mcs/

# OS
.DS_Store
Thumbs.db
